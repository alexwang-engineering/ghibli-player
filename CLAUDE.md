@AGENTS.md

## Claude Code notes

- Use `claude --worktree <task>` for implementation that runs alongside Codex.
- The `codex` MCP server is available for a second-opinion analysis or review. Keep one model as implementer and the other as reviewer instead of asking both to rewrite the same file.
- Use the project MCP servers for live library documentation and browser verification when the task needs them.
